import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  content: {
    padding: 16,
  },
  image: {
    width: '100%',
  },
  location: {
    marginTop: 16,
  },
});

export default styles;
